import "@/index.css";

// This page is fully static content (no interactive builder) — this import
// only pulls in the shared Tailwind/base stylesheet used by every public
// tool page. The page reads and works identically with JS disabled.
